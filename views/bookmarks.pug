nav.segment#elsewhere
	h2.print Links
	ul
		each bookmark in bookmarks
			- var siteUrl = bookmark.url.indexOf(homepage) === 0
			li
				a(class=bookmark.id, href=bookmark.url, rel=bookmark.rel)
					!= bookmark.iconRaw + " " + bookmark.title
